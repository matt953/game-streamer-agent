[package]
name = "gsa-client-backend-api"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false
license = "MIT OR Apache-2.0"
description = "Backend-agnostic streaming client API: the seam between a wire protocol and the shared client core"

[dependencies]
gsa-core = { workspace = true }
gsa-protocol = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
